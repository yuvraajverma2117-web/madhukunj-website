{
  "name": "madhukunj-website",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A self-contained Madhukunj food website with a native WebGL cake studio.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/browser-test.mjs",
    "check": "node --check src/app.js && node --check src/cake3d.js && npm test && npm run build"
  },
  "engines": { "node": ">=20" }
}
